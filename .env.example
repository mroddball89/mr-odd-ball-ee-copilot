# Copy to `.env` and fill in. `.env` is gitignored and must stay that way.
#
# Get a key from https://aistudio.google.com/apikey — the free tier is ~1,500 requests/day,
# which is what this project runs on.
GOOGLE_API_KEY=
